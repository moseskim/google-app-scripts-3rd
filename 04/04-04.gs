function myFunction04_04_01() {
  let x = 1;
  while (x < 100) {
    x *= 3;
    console.log(`x의 값: ${x}`);
  }
}

function myFunction04_04_02() { 
  let x = 0;
  while (x < 100) {
    x *= 3;
    console.log(`x의 값: ${x}`);
  }
}
